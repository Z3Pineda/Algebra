# Copia este archivo como .env y completa tu API key.
# ElevenLabs (preferido):
ELEVENLABS_API_KEY=
# Voz en español (opcional). Si está vacío, se usa Matías (multilingüe).
ELEVENLABS_VOICE_ID=

# OpenAI TTS (fallback si ElevenLabs falla o no hay key):
OPENAI_API_KEY=
